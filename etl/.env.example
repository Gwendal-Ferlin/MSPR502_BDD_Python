# PostgreSQL — base sante_db (microservice Santé, tables materiel + ref_exercice)
# En local avec docker compose : host localhost, port mappe 15433
# Dans le reseau Docker : host postgres-sante, port 5432
DB_HOST=localhost
DB_PORT=15433
DB_NAME=sante_db
DB_USER=sante_user
DB_PASSWORD=sante_password
