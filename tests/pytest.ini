[pytest]
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: appels HTTP réels contre API_BASE_URL (pré-prod ou local)
    slow: tests longs (ex. appel Hugging Face si RUN_IA_SMOKE=1)
